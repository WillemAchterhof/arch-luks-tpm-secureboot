# Arch Secure Installer V2 — TODO

## Next session — finish bootstrap chain

### 1. Uncomment phases in verify_repo()
In `arch_secure_install.sh` — uncomment all phases in the required array:
```bash
local required=(
    "phase_one_preboot"
    "phase_two_postboot"
    "phase_three_desktop"
    "phase_four_software"
    "lib"
    "configs"
)
```

### 2. Create placeholder main.sh files

**phase_one_preboot/main.sh**
```bash
#!/usr/bin/env bash
echo "Phase one preboot present."
exec bash "$(dirname "${BASH_SOURCE[0]}")/../phase_two_postboot/main.sh"
```

**phase_two_postboot/main.sh**
```bash
#!/usr/bin/env bash
echo "Phase two postboot present."
exec bash "$(dirname "${BASH_SOURCE[0]}")/../phase_three_desktop/main.sh"
```

**phase_three_desktop/main.sh**
```bash
#!/usr/bin/env bash
echo "Phase three desktop present."
exec bash "$(dirname "${BASH_SOURCE[0]}")/../phase_four_software/main.sh"
```

**phase_four_software/main.sh**
```bash
#!/usr/bin/env bash
echo "Phase four software present."
echo "Bootstrap chain complete."
```

### 3. Push and test
Run `update.sh` in the VM — expected output:
```
Phase one preboot present.
Phase two postboot present.
Phase three desktop present.
Phase four software present.
Bootstrap chain complete.
```

If all four lines appear — bootstrap is fully working end to end.

---

## Current repo structure
```
repo root/
├── arch_secure_install.sh
└── arch_secure_install/
    ├── phase_one_preboot/
    ├── phase_two_postboot/
    ├── phase_three_desktop/
    ├── phase_four_software/
    ├── lib/
    └── configs/
```

## Current state
- [x] arch_secure_install.sh bootstrap complete
- [x] Folder structure on GitHub correct
- [x] Clone lands cleanly at /run/sa/arch_secure_install/
- [ ] verify_repo() phases uncommented
- [ ] Placeholder main.sh files created
- [ ] Bootstrap chain tested end to end
